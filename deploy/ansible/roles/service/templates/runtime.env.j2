# Rendered by Ansible from group_vars — match-day runtime config.
HOME_ZONE={{ home_zone }}
CONTROLLER={{ controller }}
HARDWARE={{ hardware | lower }}
CAMERA={{ camera | lower }}
LOCALIZATION={{ localization | lower }}
USE_SIM_TIME={{ use_sim_time | lower }}
ROS_DOMAIN_ID={{ ros_domain_id }}
