# Inventory for the competition robot(s). Edit the address/user for your Pi 5.
# Run from repo root:
#   ansible-playbook -i deploy/ansible/inventory.ini deploy/ansible/site.yml
[pi5]
robot ansible_host=unibots-pi5.local ansible_user=unibots

[pi5:vars]
ansible_python_interpreter=/usr/bin/python3
